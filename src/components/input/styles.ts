import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 52,
    width: "100%",
    borderRadius: 10,
    borderWidth: 1,
    borderColor: "#121214",
    padding: 12,
  },
});
